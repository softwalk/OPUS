# === Database ===
DATABASE_URL=postgresql://opus:opus_dev_2026@localhost:5432/opus

# === JWT ===
# IMPORTANT: In production, generate a strong random secret:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# === Server ===
PORT=3001
NODE_ENV=development

# === CORS ===
CORS_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:5175

# === Rate Limiting (General API) ===
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=200

# === Rate Limiting (Public endpoints: orders, reservations, client login) ===
PUBLIC_RATE_LIMIT_WINDOW_MS=60000
PUBLIC_RATE_LIMIT_MAX=30

# === Logging ===
LOG_LEVEL=info
